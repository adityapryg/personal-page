---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString("id-ID", {
      month: "long",
      day: "2-digit",
      year: "numeric",
    })
  }
</time>
